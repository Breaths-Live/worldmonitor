Victor — API endpoints summary (extracted 2026-03-07)

Format: route → file → method(s) → params/body → required envs → purpose / notes

- GET /api/opensky → api/opensky.js
  - Method: GET
  - Params: any querystring passed through to relay
  - Envs: WS_RELAY_URL (required), RELAY_SHARED_SECRET (optional), RELAY_AUTH_HEADER (optional)
  - Purpose: Proxy to relay's opensky endpoint; returns flights/aircraft data. Caches via headers.

- GET /api/polymarket → api/polymarket.js
  - Method: GET
  - Params: query param `endpoint` (markets | events), other query params passed through
  - Envs: WS_RELAY_URL (optional; if absent performs direct fetch), RELAY_SHARED_SECRET (optional)
  - Purpose: Proxy to Polymarket Gamma API; does API key validation and rate limiting. Used by finance/prediction panels.

- GET /api/telegram-feed → api/telegram-feed.js
  - Method: GET
  - Params: `limit`, `topic`, `channel`
  - Envs: WS_RELAY_URL (required), RELAY_SHARED_SECRET (optional)
  - Purpose: Proxy to Telegram relay providing feed JSON (real-time signals).

- GET /api/eia/... → api/eia/[[...path]].js
  - Method: GET
  - Params: path-based; query forwarded to EIA API
  - Envs: EIA_API_KEY (required)
  - Purpose: Proxy to Energy Information Administration (energy/climate data).

- GET /api/og-story → api/og-story.js
  - Method: GET
  - Params: `c` (country code), `t` (type), `s` (score), `l` (level)
  - Envs: none required for generation
  - Purpose: Returns dynamic SVG OG image for social previews.

- GET /api/story → api/story.js
  - Method: GET
  - Params: `c`, `t`, `ts`, `s`, `l`
  - Envs: none
  - Purpose: Serves HTML meta tags for crawlers; redirects real users to SPA.

- POST /api/register-interest → api/register-interest.js
  - Method: POST
  - Body: JSON { email, source?, appVersion? }
  - Envs: CONVEX_URL (required)
  - Purpose: Register interest via Convex mutation; rate-limited per IP.

- GET /api/oref-alerts → api/oref-alerts.js
  - Method: GET
  - Params: query param `endpoint` (e.g., `history`)
  - Envs: WS_RELAY_URL (optional), RELAY_SHARED_SECRET (optional)
  - Purpose: Fetch OREF alerts or history via relay; returns 503 if no data source.

- GET /api/gpsjam → api/gpsjam.js
  - Method: GET
  - Params: none
  - Envs: none
  - Purpose: Fetch and cache GPS jamming data from gpsjam.org (CSV -> JSON), returns cached results.

- GET /api/download → api/download.js
  - Method: GET
  - Params: `platform` (required), `variant` (optional)
  - Envs: none
  - Purpose: Redirects to latest GitHub release asset for specified platform/variant.

- GET /api/rss-proxy → api/rss-proxy.js
  - Method: GET
  - Params: `url` (feed URL)
  - Envs: WS_RELAY_URL (optional), RELAY_SHARED_SECRET (optional)
  - Notes: Validates domain against `ALLOWED_DOMAINS`, may require API key (see `_api-key.js`), rate-limited.
  - Purpose: Proxy RSS feeds for news panels.

- GET /api/_health → api/_health.js
  - Method: GET
  - Params: none
  - Envs: reads a set of keys (GROQ_API_KEY, OPENROUTER_API_KEY, FINNHUB_API_KEY, FRED_API_KEY, EIA_API_KEY, ACLED_ACCESS_TOKEN, ACLED_EMAIL, WORLDMONITOR_API_KEY)
  - Purpose: Health check reporting presence of critical env keys.

- GET /api/version → api/version.js
  - Method: GET
  - Params: none
  - Envs: none
  - Purpose: Returns latest upstream release tag info for worldmonitor repo.

- GET /api/ais-snapshot → api/ais-snapshot.js
  - Method: GET
  - Params: query params passed through
  - Envs: WS_RELAY_URL (required), RELAY_SHARED_SECRET (optional)
  - Notes: validateApiKey and rate-limit apply (see `_api-key.js`, `_rate-limit.js`).
  - Purpose: Proxy to relay AIS snapshot endpoint (maritime data).

Shared middleware & notes
- `_cors.js` — CORS helpers; many handlers call `getCorsHeaders` and `isDisallowedOrigin`.
- `_api-key.js` — API key validation; some routes require API key (rss-proxy, ais-snapshot, polymarket may validate).
- `_rate-limit.js` — Rate limiting used by multiple endpoints.

Immediate risks & action items for Victor
1. Relay dependency: many endpoints require `WS_RELAY_URL` and optional `RELAY_SHARED_SECRET`. Ensure staging/production relay config is documented and present in `docs/api-matrix.md`.
2. EIA requires `EIA_API_KEY`: confirm this key is listed in docs and `.env.example`.
3. API-key protected endpoints: enumerate which endpoints require API keys and add to `docs/api-matrix.md`.
4. RSS allowed domains: `rss-proxy` has a long whitelist — update docs if adding sources.

Next step (I will do):
- Compare this list against `docs/api-matrix.md` and `docs/panel-data-map.md`, list any gaps or mismatches and prepare a PR describing additions/changes.

(Report generated by Victor agent — ready for review.)
